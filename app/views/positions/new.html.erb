<h1>Create a new position</h1>

<%= form_for([@organization, @position]) do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :description %>
    <%= f.text_field :description %>

    <%= f.submit 'Create', class: "button" %>
<% end -%>
