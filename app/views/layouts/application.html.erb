<!DOCTYPE html>
<html>
<head>
  <title>Mitto</title>
  <meta charset="utf-8">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <link rel="stylesheet" href="http://fonts.googleapis.com/css?family=Abril+Fatface|Open+Sans:400,700,400italic|Montserrat:400,700">
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <header>
    <h1><a href="/">Mitto</a></h1>
    <ul class="user-info">
      <li>
        <% if user_signed_in? -%>
          <p class="profile-picture"><%= avatar(current_user.avatar, size: "64", alt: current_user.name) %></p>
          <p class="user-name"><%= current_user.name %></p>
          <p class="logout"><%= link_to raw('&times;'), destroy_user_session_path, method: :delete, title: "Log out" %></p>
        <% end -%>
      </li>
    </ul>
  </header>

  <section class="messages">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </section>

  <div class="content">
    <%= yield %>
  </div>

</body>
</html>
